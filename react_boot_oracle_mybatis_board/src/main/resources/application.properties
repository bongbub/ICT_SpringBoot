spring.application.name=react_boot_oracle_mybatis_board

# db connection(oracle) - spring(context.xml)
spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=springboot_ict05
spring.datasource.password=tiger

# http port number(default 8080)
server.port=8081

# JPA SQL SHOW
spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect
spring.jpa.show-sql=true

# custom error page
server.error.whitelabel.enabled=false

# MyBatis - spring(dataSource-confiog.xml)
mybatis.mapper-locations=mappers/**/*.xml
mybatis.type-aliases-package=com.boot.react_board.dao

# 주의 :false를 주면 강제 변환을 막는다. 강제 변환 : bNum -> b_num, b_num -> b_num
mybatis.configuration.map-underscore-to-camel-case=false
logging.level.com.atoz_develop.mybatissample.repository=TRACE